# Optional local n8n env.
# Leave this file empty unless you explicitly need one of these:
# - WEBHOOK_URL / N8N_EDITOR_BASE_URL for a public n8n domain
# - N8N_API_URL / N8N_API_KEY for import, sync, or full MCP mode
# - CLOUDFLARED_TUNNEL_TOKEN for the Cloudflare tunnel helper
